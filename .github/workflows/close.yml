name: "unassign all when an issue is closed"
on: 
  issues:
    types:
      - closed

jobs:
  build:
    name: Build and deploy to a docker image repository
    uses: clamsproject/.github/.github/workflows/repo-issue-close.yml@main
